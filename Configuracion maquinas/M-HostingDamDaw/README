Este texto representa el archivo de configuración del servicio de Docker para Systemd (docker.service).
Su proposito principal es definir como el sistema operativo anfitrión (Linux) debe arrancar,
administrar y mantener en funcionamiento el motor de contenedores (dockerd) como un proceso en segundo plano.